import React from "react";
import Search from "./Search";

const Header = () => {
    return (
        <div>
            로고
            <Search/>
            로그인
        </div>
    );
}

// 헤더. 홈페이지의 맨 위에 들어가는 부분입니다.

export default Header;